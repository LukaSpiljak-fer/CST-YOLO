# parameters
nc: 3  # number of classes
depth_multiple: 1.0  # model depth multiple
width_multiple: 1.0  # layer channel multiple

# anchors
anchors:
  - [12,16, 19,36, 40,28]  # P3/8
  - [36,75, 76,55, 72,146]  # P4/16
  - [142,110, 192,243, 459,401]  # P5/32

# cst-yolo backbone
backbone:
  # [from, number, module, args]
  [[-1, 1, Conv, [32, 3, 1]],  # 0

   [-1, 1, Conv, [64, 3, 2]],  # 1-P1/2
   [-1, 1, Conv, [64, 3, 1]],  # 2

   [-1, 1, Conv, [128, 3, 2]],  # 3-P2/4

   [-1, 1, Conv, [64, 1, 1]],  # 4 W-ELAN 1
   [-2, 1, Conv, [64, 1, 1]],  # 5
   [-1, 1, Conv, [64, 3, 1]],  # 6
   [-1, 1, Conv, [64, 3, 1]],  # 7
   [-1, 1, Conv, [64, 3, 1]],  # 8
   [-1, 1, Conv, [64, 3, 1]],  # 9
   [[-1, -3, -5, -6], 1, MyConcat4, [1]],  # 10
   [-1, 1, Conv, [256, 1, 1]],  # 11

   [-1, 1, Conv, [256, 3, 2]],  # 12 CBSConcat (MP->Conv)
   [-1, 1, Conv, [128, 1, 1]],  # 13
   [-3, 1, Conv, [128, 1, 1]],  # 14
   [-1, 1, Conv, [128, 3, 2]],  # 15
   [[-1, -3], 1, Concat, [1]],  # 16-P3/8

   [-1, 1, Conv, [128, 1, 1]],  # 17 W-ELAN 1
   [-2, 1, Conv, [128, 1, 1]],  # 18
   [-1, 1, Conv, [128, 3, 1]],  # 19
   [-1, 1, Conv, [128, 3, 1]],  # 20
   [-1, 1, Conv, [128, 3, 1]],  # 21
   [-1, 1, Conv, [128, 3, 1]],  # 22
   [[-1, -3, -5, -6], 1, MyConcat4, [1]],  # 23
   [-1, 1, Conv, [512, 1, 1]],  # 24

   [-1, 1, Conv, [256, 3, 2]],  # 25 CBSConcat (MP->Conv)
   [-1, 1, Conv, [256, 1, 1]],  # 26
   [-3, 1, Conv, [256, 1, 1]],  # 27
   [-1, 1, Conv, [256, 3, 2]],  # 28
   [[-1, -3], 1, Concat, [1]],  # 29-P4/16
   [-1, 1, CST, [512]],  # 30 CST

   [-1, 1, Conv, [256, 1, 1]],  # 31 W-ELAN 1
   [-2, 1, Conv, [256, 1, 1]],  # 32
   [-1, 1, Conv, [256, 3, 1]],  # 33
   [-1, 1, Conv, [256, 3, 1]],  # 34
   [-1, 1, Conv, [256, 3, 1]],  # 35
   [-1, 1, Conv, [256, 3, 1]],  # 36
   [[-1, -3, -5, -6], 1, MyConcat4, [1]],  # 37
   [-1, 1, Conv, [1024, 1, 1]],  # 38

   [-1, 1, Conv, [512, 3, 2]],  # 39 CBSConcat (MP->Conv)
   [-1, 1, Conv, [512, 1, 1]],  # 40
   [-3, 1, Conv, [512, 1, 1]],  # 41
   [-1, 1, Conv, [512, 3, 2]],  # 42
   [[-1, -3], 1, Concat, [1]],  # 43-P5/32

   [-1, 1, Conv, [256, 1, 1]],  # 44 W-ELAN 1
   [-2, 1, Conv, [256, 1, 1]],  # 45
   [-1, 1, Conv, [256, 3, 1]],  # 46
   [-1, 1, Conv, [256, 3, 1]],  # 47
   [-1, 1, Conv, [256, 3, 1]],  # 48
   [-1, 1, Conv, [256, 3, 1]],  # 49
   [[-1, -3, -5, -6], 1, MyConcat4, [1]],  # 50
   [-1, 1, Conv, [1024, 1, 1]],  # 51
   [-1, 1, MCS, [1024]],  # 52 MCS
  ]

# cst-yolo neck & head
head:
  [[-1, 1, SPPCSPC, [512]],  # 53

   [-1, 1, Conv, [256, 1, 1]],  # 54 CBS
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],  # 55 Upsample
   [38, 1, Conv, [256, 1, 1]],  # 56 CBS
   [[-1, -2], 1, Concat, [1]],  # 57

   [-1, 1, Conv, [128, 1, 1]],  # 58 W-ELAN 4
   [-2, 1, Conv, [128, 1, 1]],  # 59
   [-1, 1, Conv, [128, 3, 1]],  # 60
   [-1, 1, Conv, [128, 3, 1]],  # 61
   [[-1, -2, -3, -4], 1, MyConcat4, [1]],  # 62
   [-1, 1, Conv, [256, 1, 1]],  # 63

   [-1, 1, Conv, [128, 1, 1]],  # 64 CBS
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],  # 65 Upsample
   [24, 1, Conv, [128, 1, 1]],  # 66 CBS
   [[-1, -2], 1, Concat, [1]],  # 67

   [-1, 1, Conv, [128, 1, 1]],  # 68 W-ELAN 4
   [-2, 1, Conv, [128, 1, 1]],  # 69
   [-1, 1, Conv, [128, 3, 1]],  # 70
   [-1, 1, Conv, [128, 3, 1]],  # 71
   [[-1, -2, -3, -4], 1, MyConcat4, [1]],  # 72
   [-1, 1, Conv, [256, 1, 1]],  # 73

   [-1, 1, Conv, [128, 3, 2]],  # 74 CatConv (MP->Conv)
   [-1, 1, Conv, [128, 1, 1]],  # 75
   [-3, 1, Conv, [128, 1, 1]],  # 76
   [-1, 1, Conv, [128, 3, 2]],  # 77
   [67, 1, Conv, [128, 3, 2]],  # 78 add
   [[-1,-2, -4, 73], 1, MyConcat4, [1]],  # 79

   [-1, 1, Conv, [128, 1, 1]],  # 80 W-ELAN 4
   [-2, 1, Conv, [128, 1, 1]],  # 81
   [-1, 1, Conv, [128, 3, 1]],  # 82
   [-1, 1, Conv, [128, 3, 1]],  # 83
   [[-1, -2, -3, -4], 1, MyConcat4, [1]],  # 84
   [-1, 1, Conv, [256, 1, 1]],  # 85

   [-1, 1, Conv, [256, 3, 2]],  # 86 CatConv (MP->Conv)
   [-1, 1, Conv, [256, 1, 1]],  # 87
   [-3, 1, Conv, [256, 1, 1]],  # 88
   [-1, 1, Conv, [256, 3, 2]],  # 89
   [57, 1, Conv, [256, 3, 2]],  # 90 add
   [[-1,-2, -4, 54], 1, MyConcat4, [1]],  # 91

   [-1, 1, Conv, [128, 1, 1]],  # 92 W-ELAN 4
   [-2, 1, Conv, [128, 1, 1]],  # 93
   [-1, 1, Conv, [128, 3, 1]],  # 94
   [-1, 1, Conv, [128, 3, 1]],  # 95
   [[-1, -2, -3, -4], 1, MyConcat4, [1]],  # 96
   [-1, 1, Conv, [256, 1, 1]],  # 97

   [73, 1, RepConv, [256, 3, 1]],  # 98
   [85, 1, RepConv, [512, 3, 1]],  # 99
   [97, 1, RepConv, [1024, 3, 1]],  # 100

   [[98,99,100], 1, IDetect, [nc, anchors]],   # 101 Detect(P3, P4, P5)
  ]